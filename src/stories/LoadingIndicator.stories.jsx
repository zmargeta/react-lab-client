import LoadingIndicator from '../LoadingIndicator.jsx'

const meta = {
  title: 'Loading',
  component: LoadingIndicator,
  tags: ['autodocs'],
}

const Default = {
  args: {},
}

export default meta
export { Default }
